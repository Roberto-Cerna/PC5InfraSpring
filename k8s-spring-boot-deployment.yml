apiVersion: apps/v1
kind: Deployment
metadata:
  name: roberto-springboot
spec:
  replicas: 3
  selector:
    matchLabels:
      app: roberto-springboot
  template:
    metadata:
      labels:
        app: roberto-springboot
    spec:
      containers:
        - name: springboot
          image: roberto29/roberto-docker-demo:roberto-docker-demo
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: roberto-springboot
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: roberto-springboot
